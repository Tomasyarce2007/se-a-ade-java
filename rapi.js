let domicilios = Number(prompt("cuando domicilios entregaste hoy? "))
let horasTrabajadas = Number(prompt("cuantas horas trabajaste hoy? "))

alert ("has ganado por domicilios $" + (domicilios * 2500))
alert ("tu ganancia por horas es de $" + (horasTrabajadas * 6000))
alert ("tu salario total es de $" + (horasTrabajadas * 6000 + domicilios * 2500))